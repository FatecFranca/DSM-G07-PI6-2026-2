/**
 * Placeholders Sprint 2 / 3 (não implementados na Sprint 1):
 * - GET /clusters — agrupamento K-Means das regiões (RF03.4 / RF02.2)
 * - GET /predicoes/:regiaoId — estimativa de produtividade (RF03.5 / RF02.3)
 */
export const SPRINT_2_3_ENDPOINTS = {
  clusters: 'GET /clusters',
  predicoes: 'GET /predicoes/:regiaoId',
} as const;
